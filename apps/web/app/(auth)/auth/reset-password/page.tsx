import { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Reset Password | Drovaq',
};

const LoginPage = ({
  params,
  searchParams,
}: PageProps<'/auth/reset-password'>) => {
  return <></>;
};

export default LoginPage;
